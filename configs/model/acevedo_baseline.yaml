_target_: src.models.baseline_lit_module.AcevedoBaseLineLitModule

optimizer:
  _target_: torch.optim.Adam
  _partial_: true
  lr: 0.001
  weight_decay: 0.0

scheduler:
  _target_: torch.optim.lr_scheduler.ReduceLROnPlateau
  _partial_: true
  mode: min
  factor: 0.1
  patience: 10

net:
  _target_: src.models.baseline_lit_module.BaselineModel
  in_dim: 384
  num_classes: 8 
  hidden: [256, 128, 128, 32] 
  p_drop: 0.1

# compile model for faster training with pytorch 2.0
compile: false