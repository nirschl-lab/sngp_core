# @package _global_

defaults:
  - _self_
  - data: acevedo_image_classifier # choose datamodule with `test_dataloader()` for evaluation
  - model: timm_sngp_classifier
  - callbacks: default
  - logger: wandb
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ${oc.env:HOME}/Downloads/tang_model-wniqcd8j.ckpt
